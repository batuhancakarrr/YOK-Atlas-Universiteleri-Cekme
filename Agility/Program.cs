class Program {
	static async Task Main(string[] args) {


		//using UniversityDbContext context = new();
		//Methods methods = new();



		//foreach (University university in context.Universities.AsNoTracking().ToList()) {
		//	List<UniversityDepartment> departmentList = await methods.GetDepartmentsForUniversityAsync(context, university.Id);
		//	context.UniversityDepartments.AddRange(departmentList);
		//	foreach (UniversityDepartment department in departmentList) {
		//		Console.WriteLine($"University Id: {department.UniversityId}, Department Id: {department.DepartmentId}");
		//	}
		//}
		//context.SaveChanges();



		//_ = new Methods();
		//_ = await methods.GetAllUniversitiesAsync();

		//foreach (University university in universities) {
		//	Console.WriteLine($"İsim: {university.Name}, Id: {university.Id}, Üniversite Türü: {university.Id}");
		//}

		//_ = await methods.GetAllDepartmentsAsync();

		//foreach (Department department in departments) {
		//	Console.WriteLine($"İsim: {department.Name}, Id: {department.Id}");
		//}
		//await context.SaveChangesAsync();

	}

}


